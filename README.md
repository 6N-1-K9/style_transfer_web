# Style Transfer CycleGAN - Web UI + CLI

Новый проект (с нуля) с приятным Web интерфейсом на PyWebview и полноценной консольной версией.

## Запуск

### 1) Установка зависимостей
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
