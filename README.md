# Style Transfer Web

## О проекте

Данный проект представляет собой настольное приложение для переноса визуального стиля с одного набора изображений на другие изображения с использованием архитектуры **CycleGAN**.  
Проект разработан в рамках дипломной работы и ориентирован на практическое применение методов глубокого обучения в задаче **непарного перевода изображения на изображение**.

Основная идея проекта — предоставить пользователю удобный инструмент, который позволяет:

- обучить CycleGAN-модель на двух несвязанных датасетах изображений (Domain A и Domain B);
- сохранить и переиспользовать обученные модели;
- применять перенос стиля к произвольным изображениям без необходимости писать код;
- работать как через графический интерфейс (GUI), так и через консоль (CLI).

Приложение рассчитано на пользователей с базовым пониманием машинного обучения, но не требует глубоких знаний PyTorch или GAN-архитектур.

---

## 1. Запуск проекта с нуля

Проект разрабатывался и тестировался на дистрибутиве **Arch Linux** с графической оболочкой **Gnome**. 
На других дистрибутивах Linux с другими DE, а также на Windows и macOS возможны дополнительные сложности при установке зависимостей.
При выводе графики библиотекой Pywebview используются фреймворки qt или gtk (определяются автоматически), поэтому и могут возникнуть проблемы на Windosw и MacOS.

Планируется создание версий для Windows и MacOS, чтобы пользователь мог безболезненно запустить данный проект.

### 1.1 Требования

- Python **3.9+**
- pip
- Git
- GPU с поддержкой CUDA (опционально, но крайне желательно)
- Установленные драйверы NVIDIA и CUDA Toolkit (при использовании GPU)

### 1.2 Клонирование репозитория

```bash
git clone https://github.com/6N-1-K9/style_transfer_web.git
cd <style_transfer_web>
```

### 1.3 Создание виртуального окружения

Рекомендуется использовать виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate
```

### 1.4 Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Если используется GPU, убедитесь, что версия PyTorch соответствует установленной версии CUDA.
При необходимости PyTorch можно установить вручную с официального сайта:
[https://pytorch.org](https://pytorch.org)

### 1.5 Запуск приложения

```bash
python app.py
```

После запуска будет предложено выбрать режим работы:

```
1 - Web UI (графический интерфейс)
2 - CLI (консольный режим)
```
Консольный режим является прототипом проекта с упрощенным функционалом. Данный режим использовался мной для запуска обучения на удаленном сервере.

---

## 2. Архитектура и внутренняя логика проекта

Проект логически разделён на три основных слоя:

1. Пользовательский интерфейс
2. Ядро CycleGAN
3. Вспомогательные утилиты и конфигурации

### 2.1 Общая структура проекта

```
.
├── app.py                  # Точка входа
├── webview_app.py          # Web UI (PyWebview)
├── requirements.txt
├── resize.py               # Утилита подготовки датасета
├── web/                    # HTML/CSS/JS интерфейс
│   ├── index.html
│   ├── styles.css
│   └── app.js
└── styler/                 # Ядро CycleGAN
    ├── config.py
    ├── models.py
    ├── datasets.py
    ├── trainer.py
    ├── inference.py
    ├── device.py
    └── utils.py
```

---

### 2.2 Ядро CycleGAN (папка `styler/`)

#### Модели (`models.py`)

* **StyleGenerator** — генератор на основе ResNet-блоков
* **Discriminator** — PatchGAN-дискриминатор
* **ResidualBlock** — остаточный блок генератора

Архитектура соответствует классической реализации CycleGAN.

#### Датасеты (`datasets.py`)

Используется **unpaired** датасет:

* изображения из Domain A и Domain B не имеют соответствий;
* выборка осуществляется независимо.

Все изображения приводятся к размеру `image_size` и нормализуются в диапазон `[-1, 1]`.

#### Обучение (`trainer.py`)

Класс `CycleGANTrainer` отвечает за:

* инициализацию моделей;
* настройку оптимизаторов и планировщиков;
* расчёт лоссов:

  * adversarial loss
  * cycle consistency loss
  * identity loss
* сохранение чекпоинтов и генераторов;
* возобновление обучения (resume).

Структура выходного проекта при обучении:

```
project_dir/
├── checkpoints/
├── models/
├── samples/
└── stats/
```

#### Инференс (`inference.py`)

Позволяет:

* загрузить сохранённый генератор;
* применить перенос стиля к одному изображению или папке изображений;
* сохранить результат в указанную директорию.

Генераторы сохраняются вместе с мета-информацией об архитектуре, что позволяет выполнять инференс без ручного указания параметров сети.

---

### 2.3 Пользовательский интерфейс

#### Web UI (`webview_app.py` + `web/`)

GUI реализован с помощью **PyWebview**, что позволяет:

* использовать HTML/CSS/JS;
* вызывать Python-функции напрямую из JavaScript;
* запускать обучение в отдельном потоке без блокировки интерфейса.

JavaScript взаимодействует с Python через `pywebview.api`.

#### CLI режим

CLI использует то же ядро, что и GUI, и предназначен для:

* автоматизации;
* запуска обучения на сервере без графической оболочки;
* отладки.

---

## 3. Функциональные возможности приложения

### 3.1 Выбор режима работы

* выбор графикческого интерфейла или консольного
<img width="247" height="142" alt="изображение" src="https://github.com/user-attachments/assets/b97e30d7-2a9e-41df-8ffd-bb2f0e5f138d" />

Далее будет рассмотрена только UI версия, поскольку именно она направлена на конечного пользователя.

---

### 3.2 Обучение модели (Training)

Пользователь может:

* выбрать папки Domain A и Domain B;
* задать параметры обучения:

  * размер изображений;
  * количество эпох;
  * learning rate;
  * коэффициенты лоссов;
  * количество residual-блоков;
  * использование dropout;
* выбрать устройство (CPU / CUDA);
* запустить обучение;
* остановить обучение;
* продолжить обучение с чекпоинта.

Настройки обучения:
<img width="1920" height="610" alt="изображение" src="https://github.com/user-attachments/assets/16ba90a5-2e96-4990-a2a5-a62631f8b830" />
<img width="1920" height="737" alt="изображение" src="https://github.com/user-attachments/assets/f91820f7-e1ae-4ace-8a06-38edf731c79f" />


Логи при обучении:
<img width="1920" height="461" alt="изображение" src="https://github.com/user-attachments/assets/a62eb412-80b6-46ff-9708-4a883d5540c4" />


---

### 3.3 Просмотр датасетов

Перед обучением доступен предпросмотр:

* изображений Domain A;
* изображений Domain B.

Это позволяет убедиться, что датасеты выбраны корректно. В каждом домене выводится по 3 случайных изображения из выбранного датасета. Превью Датасета представленно на фото выше.

---

### 3.4 Инференс

Пользователь может:

* выбрать обученную модель генератора;
* выбрать одно изображение или папку изображений;
* задать выходную директорию;
* выполнить перенос стиля;
* получить предпросмотр результатов.

Выбор модели для инференса и пакок для файлов:
<img width="1857" height="403" alt="изображение" src="https://github.com/user-attachments/assets/39931a9a-c8e3-4c03-a1e1-7d2c456a0ebc" />


Первые результаты:
<img width="1855" height="630" alt="изображение" src="https://github.com/user-attachments/assets/7836cb2c-0c9f-4ce5-8b92-d2630c5053c8" />


---

### 3.5 Продолжение обучения

Поскольку обучение CycleGAN модели для получения достойного результата занимает большое количество затрат по ресурсам системы и времени, то за период обучения могут произойти нештатные ситуации. Например, рабочий ПК остался без электричества, произошла внезапная перегрузка памяти или просто пользователь захотел оставновить обучение, чтобы заняться другими задачами, что повлечет за собой обрыв обучения. Чтобы не потерять прогресс была разработана система чекпоинтов с возможностью продолжить обучения с последнего сохранения.

Страница продолжения обучения:
<img width="1855" height="768" alt="изображение" src="https://github.com/user-attachments/assets/cc4e5e62-badc-4aed-8671-f3f423ad586e" />
Тут еще дополнительно должна была выводится информация с основными параметрами обучения, однако на данный момент она поломана.

---

## 4. Датасеты, на которых обучал

Для отладки и обучения были использованы датасеты:
* WIiki-Art (https://www.kaggle.com/datasets/ipythonx/wikiart-gangogh-creating-art-gan)
* ImageNet 256x256 (https://www.kaggle.com/datasets/dimensi0n/imagenet-256)
Первый датасет представляет собой огромное количество художжественных стилей, второй содержит зазличные предметы.

Для своих экспериментов рекоммендую воспользоваться этими датасетами или найти нужныйй вам на [kaggle.com](https://www.kaggle.com/)

---

## 5. Ограничения и особенности

* CycleGAN требует значительных вычислительных ресурсов;
* обучение на CPU крайне медленное;
* качество результата напрямую зависит от объёма, качества датасета;
* каждая настойка влияет на качество итоговой модели;
* GUI ориентирован на Linux-системы, на Windows/macOS возможны проблемы с PyWebview
* при слишком сложных настройках приложение может просто вылететь из-за перегрузки видео памяти или ОЗУ.

---
